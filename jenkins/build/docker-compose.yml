version: "3.3"
services:
  app:
    image: python:${BUILD_NUMBER}
    build: .
    network_mode: "host"
    env_file:
       - .env
    depends_on:
      - redis
  redis:
    image: "redis:alpine"
    network_mode: "host"
  lb:
    image: dockercloud/haproxy:latest
    links:
    - app
  
