version: "3.3"
services:
  app:
    image: "python-app:$BUILD_TAG"
    build: .
    network_mode: "host"
    env_file:
       - .env
    depends_on:
      - redis
  redis:
    image: "redis:alpine"
    network_mode: "host"
  
